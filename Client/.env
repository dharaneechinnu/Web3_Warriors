# ── Base (Shared) Frontend Environment Variables ──────────────────────
# Values here are the lowest-priority fallback.
# For local development, values are set in .env.development
# For production builds,  values are set in .env.production
#
# npm start       → uses .env.development  (localhost)
# npm run build   → uses .env.production   (ardk.online)
